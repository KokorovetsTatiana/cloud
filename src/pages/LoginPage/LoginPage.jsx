import React from 'react';
import styles from './LoginPage.module.scss';

const LoginPage = () => (
  <div className={styles.container}>
    
    <p className={styles.text}>It is LoginPage</p>
  </div>
);

export default LoginPage;